name: set-timer
order: 80
help_summary: set a timer
triggers:
  - set a timer for {duration}
  - timer for {duration}
slots:
  - name: duration
    type: duration
    required: true
    prompt: "How long should I set the timer for?"
response:
  dynamic: true
  handler: timer.set
